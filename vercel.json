{
  "framework": "vite",
  "installCommand": "npm install && npm approve-scripts esbuild",
  "buildCommand": "npx --yes vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
